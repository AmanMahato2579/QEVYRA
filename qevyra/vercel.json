{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "prisma generate && node scripts/prepare-db.mjs && next build",
  "installCommand": "npm install",
  "crons": [
    {
      "path": "/api/cron/daily-reset",
      "schedule": "15 19 * * *"
    }
  ]
}
